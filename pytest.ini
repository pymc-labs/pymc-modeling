[pytest]
testpaths = tests
pythonpath = skills/pytensor-workflows/scripts
addopts = -ra
markers =
    numba: requires the optional Numba CPU backend
    jax: requires the optional JAX CPU backend with x64 enabled
    pytorch: requires the optional PyTorch CPU backend
    mlx: requires the optional MLX CPU backend with float64 support
